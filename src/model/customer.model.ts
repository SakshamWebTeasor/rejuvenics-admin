export type CustomerModel = {
    id: string,
    name: string,
    email: string,
    phone: string,
    isAdmin: boolean,
    createdAt: string,
    updatedAt: string,
    password: string,
    image: string
};